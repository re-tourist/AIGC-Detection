# Data Directory

This directory is the repository-side landing zone for:
- dataset registry notes
- local mirrored subsets
- small checked-in manifest examples
- temporary local data preparation files

Large raw datasets should remain on the Linux server.

The active layout rule is documented in:
- `docs/data/DATA_LAYOUT.md`
